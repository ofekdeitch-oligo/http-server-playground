package main

import "testing"

func Test_Duration1_Before(t *testing.T) {
	// duration1 := NewDuration(NewDurationOptions{seconds: 60})
	// duration2 := NewDuration(NewDurationOptions{minutes: 1})

	// Test{t}.Expect(duration1.value).toEqual(duration2.value)
}
